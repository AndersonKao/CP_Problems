#include <bits/stdc++.h>
using namespace std;
#define endl '\n'


double thetaArea(double theta){

}

int main(){
    int a1, b1, a2, b2, t1, t2, t;
    int cnt = 1;
    while (cin >> a1 >> b1 >> t1 >> a2 >> b2 >> t)
    {
        if(!a1 && !b1 && !t1 && !a2 && !b2 && !t)
            break;
        double t2 =    
        cout << "Solar System " << cnt++ << ": " << endl;
        
    }

    return 0;
}
