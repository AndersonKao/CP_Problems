#include <bits/stdc++.h>
using namespace std;
int H; // Number of holes
int N; // Number of vertices
vector<tuple<int, int, int, int>> holes; // x,y,z 
void solve(){
    N = H + 2;
    holes.resize(H);
}
int main(){
    cin.tie(0);
    ios_base::sync_with_stdio(false);

    while(cin >> H){
        if(H == -1)
            break;
        for (int i = 0; i < H; i++){

        }
            for (int i = 0; i < N; i++)
            {
            }
        solve();
    }
    return 0;
}