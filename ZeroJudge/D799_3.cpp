// Square Root Technique
#include <bits/stdc++.h>
using namespace std;
int seq[500000];
int main(){
    int n, q;
    cin >> n >> q;
    for(int i = 0; i < n; i++){
        cin >> seq[i];
    }

    int sqr = (int)sqrt(n) - 1;
    

    return 0;
}