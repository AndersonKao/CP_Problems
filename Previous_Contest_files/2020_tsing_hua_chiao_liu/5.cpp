#include <iostream>
#include <algorithm>
using namespace std;
int seq[100005];
void solve(int index){
    
}
int main(){
    int N;
    cin >> N;
    for (int i = 0; i < N; i++){
        cin >> seq[i];
    }
    sort(seq, seq + N);
    for (int i = N - 1; i >= 3; i++){

    }
        return 0;
}